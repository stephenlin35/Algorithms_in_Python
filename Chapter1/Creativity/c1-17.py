"""
    This does not change the values in the data list because val in the for-loop is assigned to an element in the data array, but that's all it is, a variable. In the statement, val *= factor, it assigns val to a different object, thereby not affecting the values in the data array. """
